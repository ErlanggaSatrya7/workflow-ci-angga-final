global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'ml_model_exporter'
    static_configs:
      # Ubah dari localhost menjadi host.docker.internal
      - targets: ['host.docker.internal:8001']